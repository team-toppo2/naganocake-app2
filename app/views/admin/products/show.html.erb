<h2>商品詳細</h2>
<div>
  <%= attachment_image_tag @product, :image %>
  <p>商品名    <%= @product.name %></p>
  <p>商品説明     <%= @product.discription %></p>
  <p>ジャンル    <%= @product.genre.name %></p>
  <p>税込価格<br>(税抜価格)    <%= @product.price*1.1.to_s(:delimited) %> (<%= @product.price.to_s(:delimited) %>)円</p>
  <p>販売ステータス
    <% if @product.is_active == true %>
      <p>販売中</p>
    <% else %>
      <p>販売停止中</p>
    <% end %>
  </p>
  <div>
    <%= link_to '編集する',edit_admin_product_path(@product) %>
  </div>
</div>